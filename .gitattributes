# Normalize line endings across platforms.
# Text files use LF in the repo; Git converts on checkout as needed.
* text=auto

# Source and data files: always LF in the repo.
*.mjs   text eol=lf
*.js    text eol=lf
*.json  text eol=lf
*.md    text eol=lf
